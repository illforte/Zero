version: '3.8'

services:
  mail-zero:
    image: ghcr.io/lair404xyz/mail-zero:latest
    container_name: mail-zero
    restart: always
    init: true
    env_file: .env
    environment:
      NODE_ENV: production
    command: ["/bin/sh", "-c", "/app/scripts/replace-placeholder.sh 'http://REPLACE-BACKEND-URL.com' '' && /app/scripts/replace-placeholder.sh 'http://REPLACE-APP-URL.com' 'https://mail.n1njanode.com' && cd /app/apps/mail/build/client && exec npx --yes serve -s -l 3000"]
    depends_on:
      mail-zero-server:
        condition: service_started
    ports:
      - "127.0.0.1:3050:3000"
    healthcheck:
      test: ['CMD', 'wget', '--spider', '--quiet', 'http://127.0.0.1:3000']
      interval: 90s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      - mail-zero-net
    labels:
      - "com.n1njanode.service=mail-zero"
      - "com.n1njanode.port=3050"

  mail-zero-server:
    image: ghcr.io/lair404xyz/mail-zero-server-node:latest
    container_name: mail-zero-server
    restart: always
    init: true
    env_file: .env
    network_mode: host
    environment:
      NODE_ENV: production
      PORT: "3051"
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@127.0.0.1:5436/${POSTGRES_DB}
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET:?required}
      BETTER_AUTH_URL: https://mail-api.n1njanode.com
      CF_ACCESS_AUD: ${CF_ACCESS_AUD:?required}
      CF_ACCESS_TEAM_DOMAIN: weretrade.cloudflareaccess.com
      IMAP_PROXY_URL: http://127.0.0.1:3060
      IMAP_URL: ${IMAP_URL:?required}
      SMTP_URL: ${SMTP_URL:?required}
      APP_URL: https://mail.n1njanode.com
      CORS_ORIGINS: https://mail.n1njanode.com
      COOKIE_DOMAIN: .n1njanode.com
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:?required}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:?required}
    depends_on:
      mail-zero-db:
        condition: service_healthy
      mail-zero-imap-proxy:
        condition: service_healthy
    healthcheck:
      test: ['CMD', 'wget', '--spider', '--quiet', 'http://127.0.0.1:3051/health']
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    labels:
      - "com.n1njanode.service=mail-zero-server"
      - "com.n1njanode.port=3051"

  mail-zero-imap-proxy:
    image: ghcr.io/lair404xyz/mail-zero-imap-proxy:latest
    container_name: mail-zero-imap-proxy
    restart: always
    init: true
    env_file: .env
    network_mode: host
    environment:
      PORT: "3060"
    healthcheck:
      test: ['CMD', 'wget', '--spider', '--quiet', 'http://127.0.0.1:3060/health']
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    labels:
      - "com.n1njanode.service=mail-zero-imap-proxy"
      - "com.n1njanode.port=3060"

  mail-zero-mcp:
    image: ghcr.io/lair404xyz/mail-zero-server-node:latest
    container_name: mail-zero-mcp
    restart: always
    init: true
    env_file: .env
    network_mode: host
    environment:
      NODE_ENV: production
      PORT: "5008"
      MCP_TRANSPORT: "sse"
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@127.0.0.1:5436/${POSTGRES_DB}
    command: ["node", "dist/mcp-server.js"]
    depends_on:
      mail-zero-db:
        condition: service_healthy
    healthcheck:
      test: ['CMD', 'wget', '--spider', '--quiet', 'http://127.0.0.1:5008/health']
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    labels:
      - "com.n1njanode.service=mail-zero-mcp"
      - "com.n1njanode.port=5008"

  mail-zero-db:
    image: postgres:17-alpine
    container_name: mail-zero-db
    restart: always
    init: true
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-mailzero}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?required}
      POSTGRES_DB: ${POSTGRES_DB:-mailzero}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - mail-zero-postgres:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5436:5432"
    healthcheck:
      test: ['CMD', 'pg_isready', '-U', '${POSTGRES_USER:-mailzero}', '-d', '${POSTGRES_DB:-mailzero}']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - mail-zero-net
    labels:
      - "com.n1njanode.service=mail-zero-db"

  mail-zero-valkey:
    image: docker.io/bitnami/valkey:latest
    container_name: mail-zero-valkey
    restart: always
    init: true
    environment:
      - ALLOW_EMPTY_PASSWORD=yes
      - VALKEY_DISABLE_COMMANDS=FLUSHDB,FLUSHALL
    volumes:
      - mail-zero-valkey:/bitnami/valkey/data
    healthcheck:
      test: ['CMD', 'redis-cli', '-h', 'localhost', '-p', '6379', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - mail-zero-net
    labels:
      - "com.n1njanode.service=mail-zero-valkey"

  mail-zero-upstash-proxy:
    image: hiett/serverless-redis-http:latest
    container_name: mail-zero-upstash-proxy
    restart: always
    init: true
    environment:
      SRH_MODE: env
      SRH_TOKEN: ${REDIS_TOKEN:?required}
      SRH_CONNECTION_STRING: 'redis://mail-zero-valkey:6379'
    healthcheck:
      test: ['CMD', 'wget', '--spider', '--quiet', 'http://127.0.0.1:80']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - mail-zero-net
    labels:
      - "com.n1njanode.service=mail-zero-upstash-proxy"

networks:
  mail-zero-net:
    driver: bridge

volumes:
  mail-zero-postgres:
    name: mail-zero-postgres
  mail-zero-valkey:
    name: mail-zero-valkey
